import { counterSlice, counterReducer } from "./model/slice/counterSlice";
import { Counter } from "./ui/counter";
import { CounterScheme } from "./model/types/counterScheme";

export { counterSlice, counterReducer, Counter, CounterScheme };
